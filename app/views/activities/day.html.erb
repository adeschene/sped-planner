<%# Disable Turbo page caching for this page %>
<% content_for(:body_attributes) do %>
  data-turbo-cache="false"
<% end %>

<div style="max-width: 900px; margin: 0 auto;">

<%= calendar number_of_days: 1, events: @activities do |date, activities| %>
  <% @timeslots.each do |timeslot| %>
    <% @filtered = activities.select{|a| a.block == timeslot.position} %>
    <% @collapseID = "blockID" + timeslot.label.to_s.sub(/:/, '_') %>
      <% unless @filtered.empty? %>
        <div class="list-group mb-3 mx-3">
          <a href="#<%= @collapseID %>" data-bs-toggle="collapse" class="list-group-item list-group-item-active fs-5 fw-bold block-header text-center border-dark <%= block_css_class(timeslot.position) %>" aria-expanded="true" aria-controls="<%= @collapseID %>">
            <%= timeslot.label %>
          </a>
          <div id="<%= @collapseID %>" class="collapse show">
            <% @filtered.each do |activity| %>
              <%= link_to activity.title, activity_path(activity, ref: 'day'), class: "list-group-item list-group-item-action list-group-item-secondary block-item border-dark" %>
            <% end %>
          </div>
        </div>
      <% end %>
    <% end %>
<% end %>

<div class="card text-white bg-dark bg-gradient mb-3">
  <div class="card-header text-center fw-semibold">
    <i class="bi bi-plus-circle me-1"></i>Add Activity
  </div>
  <div class="card-body form-body">
    <%= render 'form', activity: Activity.new %>
  </div>
</div>

</div><%# end max-width wrapper %>