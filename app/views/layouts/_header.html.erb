<nav class="navbar navbar-dark bg-dark bg-gradient py-2">
  <div class="container-fluid position-relative d-flex align-items-center">

    <div class="position-absolute start-50 translate-middle-x d-flex gap-2">
      <% if request.path == month_view_path %>
        <span class="btn btn-sm rounded-pill btn-light" aria-current="page">
          <i class="bi bi-calendar-month me-1"></i>Month
        </span>
      <% else %>
        <%= link_to month_view_path(start_date: params[:start_date]),
              class: "btn btn-sm rounded-pill btn-outline-light" do %>
          <i class="bi bi-calendar-month me-1"></i>Month
        <% end %>
      <% end %>

      <% if request.path == week_view_path %>
        <span class="btn btn-sm rounded-pill btn-light" aria-current="page">
          <i class="bi bi-calendar-week me-1"></i>Week
        </span>
      <% else %>
        <%= link_to week_view_path(start_date: params[:start_date]),
              class: "btn btn-sm rounded-pill btn-outline-light" do %>
          <i class="bi bi-calendar-week me-1"></i>Week
        <% end %>
      <% end %>

      <% if request.path == day_view_path %>
        <span class="btn btn-sm rounded-pill btn-light" aria-current="page">
          <i class="bi bi-calendar-day me-1"></i>Day
        </span>
      <% else %>
        <%= link_to day_view_path(start_date: params[:start_date]),
              class: "btn btn-sm rounded-pill btn-outline-light" do %>
          <i class="bi bi-calendar-day me-1"></i>Day
        <% end %>
      <% end %>
    </div>

    <div class="ms-auto">
      <% if request.path == timeslots_path %>
        <span class="btn btn-sm rounded-pill btn-light" aria-current="page">
          <i class="bi bi-gear me-1"></i>Settings
        </span>
      <% else %>
        <%= link_to timeslots_path, class: "btn btn-sm rounded-pill btn-outline-light" do %>
          <i class="bi bi-gear me-1"></i>Settings
        <% end %>
      <% end %>
    </div>

  </div>
</nav>
